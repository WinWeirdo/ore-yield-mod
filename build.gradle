plugins {
    id 'dev.architectury.loom' apply false
    id 'net.minecraftforge.gradle' apply false
    id 'com.gradleup.shadow' apply false
    id 'net.neoforged.moddev' apply false
}

subprojects {
    group = 'com.oreyield'
    version = rootProject.findProperty('mod_version') ?: '1.1.0'
}
