package com.oreyield.neoforge.event;

/**
 * Kept as a compatibility placeholder for older installations.  XP is now
 * awarded by {@code OreYieldLootModifier}, where it is coupled to successful
 * loot generation rather than a cancelable pre-break event.
 */
public final class BreakXpHandler {
    private BreakXpHandler() {}
}
