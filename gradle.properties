org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Mod
mod_version = 1.1.0
maven_group = com.oreyield

# Build tooling only. The version matrix itself lives in versions.json;
# active MC version is chosen via -Pmc=... or the MC_VERSION env var.
forgegradle_version = 6.0.54
loom_version = 1.10.455
moddev_version = 2.0.142
shadow_version = 8.3.6
